import { Language,tr } from "@/lib/i18n";
export default function Services({language}:{language:Language}) {const t=tr(language);const services=[["♃",t.career,t.careerText],["♀",t.relationships,t.relationshipsText],["☽",t.general,t.generalText]];return <section className="services" id="services"><div className="wrap"><h2>{t.servicesTitle}</h2><p>{t.servicesText}</p><div className="service-grid">{services.map(([glyph,title,description])=><div className="service-card" key={title}><span className="glyph">{glyph}</span><h3>{title}</h3><p>{description}</p><span className="price">₹500 / {t.perSession}</span></div>)}</div></div></section>;}